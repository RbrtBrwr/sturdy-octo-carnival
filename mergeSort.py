# def mergeSort(iterable):
#     if len(iterable) > 1:
#         size = len(iterable)
#         half = size // 2
#         iterableA = iterable[0,half]
#         iterableB = iterable[half+1, size]
#         arrA = mergeSort(iterableA)
#         arrB = mergeSort(iterableB)
#         newArr = []
#         for i in range(len(arrA)):
#             for j in range(len(arrB)):
#                 if arrA > 
